name: Surge Relay
options:
  bundleIdPrefix: com.allenmiao
  developmentLanguage: zh-Hans
  deploymentTarget:
    macOS: "26.0"
  groupSortPosition: top
  generateEmptyDirectories: true
settings:
  base:
    SWIFT_VERSION: "6.0"
    MACOSX_DEPLOYMENT_TARGET: "26.0"
    CURRENT_PROJECT_VERSION: 202609100000
    MARKETING_VERSION: 202609100000
    CODE_SIGN_STYLE: Automatic
    ENABLE_USER_SCRIPT_SANDBOXING: YES
packages:
  Sparkle:
    url: https://github.com/sparkle-project/Sparkle
    from: 2.9.3
targets:
  Surge Relay:
    type: application
    platform: macOS
    deploymentTarget: "26.0"
    info:
      path: SurgeRelay/Info.plist
      properties:
        CFBundleDevelopmentRegion: zh-Hans
        CFBundleLocalizations:
          - zh-Hans
        CFBundleDisplayName: Surge Relay
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        LSApplicationCategoryType: public.app-category.utilities
        NSAppTransportSecurity:
          NSAllowsArbitraryLoads: true
        SUFeedURL: https://raw.githubusercontent.com/Primovist/SurgeRelay-macOS/main/appcast.xml
        SUPublicEDKey: $(SPARKLE_PUBLIC_ED_KEY)
        SUEnableAutomaticChecks: true
    sources:
      - path: SurgeRelay
        excludes:
          - WebResources
      - path: SurgeRelay/WebResources
        type: folder
        buildPhase: resources
      - path: Surge Relay.icon
        buildPhase: resources
        optional: true
    dependencies:
      - sdk: JavaScriptCore.framework
      - package: Sparkle
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.allenmiao.SurgeRelay
        PRODUCT_NAME: Surge Relay
        PRODUCT_MODULE_NAME: SurgeRelay
        ASSETCATALOG_COMPILER_APPICON_NAME: Surge Relay
        ENABLE_APP_SANDBOX: NO
        SWIFT_STRICT_CONCURRENCY: complete
        ARCHS: arm64
  Surge RelayTests:
    type: bundle.unit-test
    platform: macOS
    deploymentTarget: "26.0"
    sources:
      - path: SurgeRelayTests
    dependencies:
      - target: Surge Relay
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.allenmiao.SurgeRelayTests
        GENERATE_INFOPLIST_FILE: YES
